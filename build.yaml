targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: cozy_world|lib/data/datasources/remote/graphql/schema/schema.graphql
          output_dir: 'generated'
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: cozy_world|lib/data/datasources/remote/graphql/schema/schema.graphql
          output_dir: 'generated'
