query TodayMessage($day: Date!) {
  messagesCollection(
    filter:
        {on_day: {eq: $day}},
  ) {
    edges {
      node {
        message_id
        on_day
        type
        content
      }
    }
  }
}
