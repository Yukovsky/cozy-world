query CategoryMessages($category: String, $read: Boolean) {
  messagesCollection(
    filter:
      {category: {eq: $category},
       is_read: {eq: $read}}
  ) {
    edges {
      node {
        message_id
        type
        content
        category
      }
    }
  }
}
